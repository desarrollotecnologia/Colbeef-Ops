# Configuración Streamlit — Inventario Colbeef
# Acceso en LAN: http://<IP-de-esta-PC>:8501

[server]
address = "0.0.0.0"
port = 8501
# false = intenta abrir el navegador en esta PC al iniciar (recomendado con Laragon / escritorio).
headless = false

[browser]
gatherUsageStats = false
